[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
name = "cdis2java"
version = "0.0.1a0"
requires-python = ">=3.10"
description = "convert cdis bytecode to JVM bytecode"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICEN[CS]E*"]
keywords = ["dis", "cdis", "disassembly", "code generation", "code analysis"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Compilers",
    "Topic :: Software Development :: Code Generators",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
  "cdis~=0.0.1a0",
  "jpype1~=1.6.0"
]

[project.urls]
Homepage = "https://github.com/cdis-vm/cdis2java/tree/main"
Repository = "https://github.com/cdis-vm/cdis2java.git"
Issues = "https://github.com/cdis-vm/cdis2java/issues"


[dependency-groups]
dev = ["pytest>8", "coverage", "tox", "ruff"]


[pytest]
testpaths="tests"


[tool.ruff]

[tool.ruff.lint.per-file-ignores]
# __init__ files may use star imports, since they may be merging several sub_modules
# which each define an appropiate __all__
"__init__.py" = ["F403"]
